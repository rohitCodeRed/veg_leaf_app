import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'veg_leaf_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
